using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewPatrol : MonoBehaviour
{
    /*

    public float speed = 0.1f;
    float timer = 0f;

    // Use this for initialization
    void Start()
    {

    private Vector3 pos1 = new Vector3(transform.position.x - 4, transform.position.y, transform.position.z);
    private Vector3 pos2 = new Vector3(transform.position.x + 4, transform.position.y, transform.position.z);

    }

// Update is called once per frame
void Update()
{
    transform.position = Vector3.Lerp(pos1, pos2, Mathf.PingPong(Time.time / 3 * speed, 1.0f));

    timer += Time.deltaTime;

    if (timer >= 3)
    {
        timer = 0;
        transform.localScale = new Vector3(-transform.localScale.x, transform.localScale.y, transform.localScale.z);
    }
}
*/
}
